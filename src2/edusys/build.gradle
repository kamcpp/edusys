subprojects {
    apply plugin: 'java'
    apply plugin: 'maven'

    sourceCompatibility = 1.7
    version = '1.0'

    repositories {
        mavenLocal()
        maven {
            url 'http://localhost:8081/nexus/content/groups/public'
        }
    }
}